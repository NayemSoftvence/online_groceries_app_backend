[build]
  publish = "public"
  command = "npm install"

[build.environment]
  NODE_ENV = "production"

[functions]
  directory = "netlify/functions"

# Redirect all API routes to our single function
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200

# Serve index.html for all other routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200