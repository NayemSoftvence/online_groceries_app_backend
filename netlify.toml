[build]
  publish = "public"
  command = "echo 'No build needed'"

[build.environment]
  NODE_ENV = "production"

[functions]
  directory = "netlify/functions"

# Redirect all API routes to the api function
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200

# Serve index.html for all other routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200